# 文档 / ADR

- **ADR 记 why**：框架选型、数据模型、auth、API 架构、任何"昂贵到难回退"的决策。
- 模板：Status + Date + Context + Decision + Alternatives(pros/cons/弃因) + Consequences；存 `docs/decisions/`、编号、不删旧的（新 ADR supersede）。
- 注释只写"为什么"不写"是什么"；陷阱标 `[IMPORTANT]` 并引 ADR；注释掉的死代码全删。
- README 必备：quick start + 命令表 + 架构 + 贡献流程。
