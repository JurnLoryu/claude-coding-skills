# 规格（spec，写码的闸门）

- **先列假设**（web/native、auth、DB、浏览器支持）逼用户立刻纠偏。
- 6 域：Why(目标) / Commands / 项目结构 / 代码风格 / 测试策略 / 边界(Always·Ask·Never)。
- 把模糊需求转**可测**（"快"→ LCP<2.5s + 加载<500ms + CLS<0.1）。
- Spec 是活文档：决策变 → 先改 spec 再改码。
