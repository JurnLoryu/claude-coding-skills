# 规划（planning）

- 先进"计划模式"（只读）：梳理 spec、找现成 pattern、画**依赖图**。
- **纵向切片**而非横向：不是"整 DB→整 API→整 UI"，而是"注册(schema+API+UI)→登录(…)→建任务(…)"，每片可独立验证。
- 每个任务：描述 + ≤3 条验收标准 + 验证步骤 + 依赖 + 涉及文件 + 规模(S/M)；单会话(≤2h)、碰 ≤5 文件。
- 每 2–3 任务设 **checkpoint**（测试过、编译过、端到端 work、人工评审）。
