# 源码驱动（动手前）

- 改前读 `package.json`/`requirements.txt` 等**确定版本**，别猜；版本不明就问。
- 只认**官方文档**为权威（react.dev、docs.djangoproject.com…），不认 SO/博客/AI 总结；冲突取当前版本文档。
- 实现后**附源链接 + 版本号**，最好用带锚点的深链（`/reference/...#usage`）。
- 找不到官方文档就明说"未验证"，别模棱两可。
