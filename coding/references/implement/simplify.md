# 简化（合并前扫一遍，降认知复杂度）

- 改前答 **Chesterton 五问**：这段做什么 / 谁调它 / 边界情况 / 为啥这样写 / git blame 说啥。答不上别改。
- 按**具体信号**改，不靠"代码味"：深嵌套(3+) → 护卫子句；长函数(50+) → 拆专注函数；嵌套三元 → if/查表。
- 明确命名 > 聪明代码；保持项目既有风格（先看 CLAUDE.md/邻近范本）。
- 改一处 → 跑测 → 过了再 commit；批量改用自动化。
- **检验**：简化版真更好读吗？有没有引入不合项目的新模式？diff 干净吗？队友会批准吗？否则回滚。

> 注：纯"格式统一 + 去冗余、不改逻辑"另见 `code-polish` 技能；本篇偏降低逻辑/认知复杂度。
