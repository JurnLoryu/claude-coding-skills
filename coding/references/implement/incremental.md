# 增量实现

- 每步 **≤100 行、只改一件事**，改完就**编译 + 测试 + 手工验证**。
- **不跨边界**：不顺手清理外围、不改 import 顺序、不删无关注释、不加 spec 外的功能。
- 三项全绿（编译过 / 现有测试过 / 新功能 work）才 commit。
- 未完成功能用**功能开关**藏起来，别裸合并到 main。
- 大重构（>500 行）用 codemod/脚本，别手工改。
